{
  "name": "contracts",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "solc": "^0.8.28"
  },
  "scripts": {
    "compile": "solcjs --abi --bin --output-dir dist"
  }
}
